import 'package:flutter/material.dart';

class SetupAgeScreen extends StatelessWidget {
  const SetupAgeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('setup age....'),
        ),
        body: Column(
          children: [],
        ));
  }
}
